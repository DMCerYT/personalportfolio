import BasedCard from "../components/BasedCard";

export default function Projects() {
    return (
        <div>
            <BasedCard header="Current Projects" text="These are some projects I am currently working on!" />
            <BasedCard header="Previous Projects" text="These are projects I have previously contributed on" />
        </div>
    );
}